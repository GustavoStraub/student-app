import { Text, View } from "react-native";

export default function Resumos() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Testando</Text>
    </View>
  );
}
